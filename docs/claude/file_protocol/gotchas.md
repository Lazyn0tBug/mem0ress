---
task_id: "{task_id}"
type: gotchas
---

# Gotchas — {task_id}

> 偏差与经验记录，带外追加，不阻塞主流程。
> 每条 Gotcha 独立成块，只追加不覆盖。
> Judge Agent 在 Tier 0 检验时会读取此文件作为约束违反的参照依据。

---

## Gotcha {G-N} — {简短标题}
**Timestamp:** YYYY-MM-DDTHH:mm:ssZ
**Turn:** {关联轮次，如 1.2}

### 触发背景
{在执行哪个 Todo 或验证哪个 Requirement 时发现的问题。一两句话。}

### 实际发生了什么
{具体问题描述。不要写"发现了问题"，要写"由于 X，导致 Y，表现为 Z"。}

### 如何处理
{最终决定是什么：修改了执行路径？追加了子任务？让度给人确认？修改了 Requirements？
写结论，不写过程。}

---
